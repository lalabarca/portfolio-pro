<%= render 'shared/home_navbar' %>
<section id="header">
  <div class="container">
    <div class="row">
      <div class="col-12 col-lg-9">
        <h1>
          <span>Web</span>
          <span>design</span><span>&</span>
          <span>web apps</span>
        </h1>
      </div>
      <div class="col-12 col-lg-3">
        <div class="hello">
          <p>Hello 👋🏾</p>
          <h2>Je m'appelle <em>Laura</em> et je suis infographiste, webdesigner et webdéveloppeuse</h2>
        </div>
      </div>
      <div class="separator"></div>
      <div class="illu"></div>
    </div>
  </div>
</section>
<section id="services">
  <div class="container">
    <div class="row">
      <div class="col-12 col-lg-6">
        <article>
          <h2>Ui Design</h2>
          <p>Passionée par le graphisme et le design depuis toute petite, j'ai commencé à travailler en tant qu'infographiste puis je me suis tourné vers le webdesign après une formation de Chef de projet multimédia.</p>
          <p>Le petit <span class="fe fe-plus-circle"></span> : la veille pour toujours être au courant des dernières tendances, le travail avec les typographies et les illustrations, la construction des maquettes et prototypes</p>
        </article>

        <div>
          <% @skills_design.each do |skill| %>
            <div class="card">
  <div class="card-body">
    <div class="row align-items-center">
      <div class="col-auto">
      <!-- Avatar -->
        <figure class="skill-icons">
          <% if skill.photo.attached? %>
            <%= cl_image_tag skill.photo.key, class: "avatar-img rounded", alt: "icon de compétence" %>
          <% else %>
            <%= image_tag "skill_icon.svg", class: "avatar-img rounded", alt: "icon de compétence"  %>
          <% end %>
        </figure>
      </div>
      <div class="col ms-n2">
        <!-- Title -->
        <h4 class="mb-1">
          <%= skill.title %>
        </h4>

        <!-- Progress -->
        <div class="row align-items-center g-0">
          <div class="col">

            <!-- Progress -->
            <div class="skill-levels">
              <%= render 'admin/partials/skill_levels', skill: skill %>
            </div>

          </div>
        </div> <!-- / .row -->
      </div>
    </div>
  </div> <!-- / .row -->
</div> <!-- / .card-body -->

          <% end %>
        </div>
      </div>
      <div class="col-12 col-lg-6">
        <div>
          <% @skills_web.each do |skill| %>
            <div class="card">
  <div class="card-body">
    <div class="row align-items-center">
      <div class="col-auto">
      <!-- Avatar -->
        <figure class="skill-icons">
          <% if skill.photo.attached? %>
            <%= cl_image_tag skill.photo.key, class: "avatar-img rounded", alt: "icon de compétence" %>
          <% else %>
            <%= image_tag "skill_icon.svg", class: "avatar-img rounded", alt: "icon de compétence"  %>
          <% end %>
        </figure>
      </div>
      <div class="col ms-n2">
        <!-- Title -->
        <h4 class="mb-1">
          <%= skill.title %>
        </h4>

        <!-- Progress -->
        <div class="row align-items-center g-0">
          <div class="col">

            <!-- Progress -->
            <div class="skill-levels">
              <%= render 'admin/partials/skill_levels', skill: skill %>
            </div>

          </div>
        </div> <!-- / .row -->
      </div>
    </div>
  </div> <!-- / .row -->
</div> <!-- / .card-body -->

          <% end %>

         <div class="card">
  <div class="card-body">
    <div class="row align-items-center">
      <div class="col-auto">
      <!-- Avatar -->
        <figure class="skill-icons">
          <% if skill.photo.attached? %>
            <%= cl_image_tag skill.photo.key, class: "avatar-img rounded", alt: "icon de compétence" %>
          <% else %>
            <%= image_tag "skill_icon.svg", class: "avatar-img rounded", alt: "icon de compétence"  %>
          <% end %>
        </figure>
      </div>
      <div class="col ms-n2">
        <!-- Title -->
        <h4 class="mb-1">
          <%= skill.title %>
        </h4>

        <!-- Progress -->
        <div class="row align-items-center g-0">
          <div class="col">

            <!-- Progress -->
            <div class="skill-levels">
              <%= render 'admin/partials/skill_levels', skill: skill %>
            </div>

          </div>
        </div> <!-- / .row -->
      </div>
    </div>
  </div> <!-- / .row -->
</div> <!-- / .card-body -->

        </div>
        <article>
          <h2>Développement web</h2>
          <p>J'ai découvert d'abord le métier d'intégrateur web, qui m'a tout de suite plu. Mais j'ai surtout eu l'opportunité de me former à Ruby on rails et à Javascript en intégrant le batch de Conceptrice - Développeuseuse d’Applications Web, de l'école <em>Le Wagon</em>.</p>
          <p>Le petit <span class="fe fe-plus-circle"></span> : on peut se former facilement à de nouvelles technologies, les languages de programmation sont universels, lorsqu'on code on a tout de suite une vue du résultat final contrairement à d'autres corps de métiers </p>
        </article>
      </div>
    </div>
  </div>
</section>
<section id="projects">
  <div class="container">
    <h2>Mes derniers projets</h2>
    <div class="cards">
    <% @projects.each do |project| %>
      <div class="card-project" data-color="<%= %w(white yellow white yellow).sample %>">
        <% if project.photos.attached? %>
          <!-- Image -->
          <%= link_to project_path(project) do %>
            <figure>
              <%= cl_image_tag project.photos[0].key  %>
            </figure>
          <% end %>
          <h3>
          <%= link_to project_path(project) do %>
            <%= project.title %>
          <% end %>
          </h3>
        <% end %>
      </div>
    <% end %>
    </div>
  </div>
</section>
<section id="contact">
  <div class="container">
    <h2>Me contacter</h2>
    <p><%= mail_to "laura.hardydes@gmail.com", "laura.hardydes@gmail.com" %></p>
    <p>N'hésitez pas à me contacter pour avoir plus d'informations sur mon travail, mon parcours, ce que vous voulez... 😁</p>
  </div>
</section>
<%= render "shared/footer" %>
